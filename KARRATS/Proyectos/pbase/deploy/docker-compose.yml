services:
  web:
    build: 
      context: ${CONTEXTO}
      #args: #variables en dockerfile para la construcción de la imagen
      #  - USUARIO=pedro
      dockerfile: ./dockerfiles/base/ubbase
    #image: cristian/ubbase
    container_name: ${CONTENEDOR}
    ports:
      - "${SSH_PORT}:${SSH_PORT}"
    env_file:
      - ./.env
    volumes:
      - ./logs:/root/logs
    networks:
      netkarrats:
          ipv4_address: ${IP}

networks:
  netkarrats:
    ipam:
      driver: default
      config:
        - subnet: ${SUBNET}